server:
  port: ${SHOP_RULES_GENERATOR_PORT}

spring:
  data:
      mongodb:
        host: ${MONGO_IP}
        port: ${MONGO_PORT}
        username: ${MONGO_URLCLEANER_USER}
        password: ${MONGO_PASSWORD}
        database: data